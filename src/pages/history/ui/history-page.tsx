import { NavLink } from 'react-router-dom'

const HistoryPage = () => {
    return (
        <div>
            <NavLink to={'/signin'}>favorites</NavLink>
        </div>
    )
}

export default HistoryPage
